import 'package:flutter/material.dart';

//Define constant colors
const Color mainColor = Color.fromARGB(255, 255, 119, 0);
// const Color mainColor = Color.fromARGB(255, 16, 146, 137);
const Color greyColor = Colors.grey;
const Color whiteColor = Colors.white;
const Color blackColor = Color.fromARGB(255, 0, 0, 0);
const Color textColor = Color.fromARGB(255, 0, 0, 0);
const Color blueColor = Color.fromARGB(255, 193, 188, 165);
const Color bgColor = Color.fromRGBO(17, 99, 89, 1);

// Define padding and margin constants
const EdgeInsets defaultPadding = EdgeInsets.all(16.0);
const EdgeInsets smallPadding = EdgeInsets.all(8.0);
const EdgeInsets largePadding = EdgeInsets.all(24.0);

// Define size constants
const double smallHeight = 50.0;
const double mediumHeight = 100.0;
const double largeHeight = 150.0;

// Define text style constants
const TextStyle titleStyle = TextStyle(
  fontSize: 24.0,
  fontWeight: FontWeight.bold,
  color: textColor,
);

const TextStyle boldStyle = TextStyle(
  fontSize: 20.0,
  fontWeight: FontWeight.bold,
  color: textColor,
);

const TextStyle bodyStyle = TextStyle(
  fontSize: 16.0,
  color: textColor,
);
